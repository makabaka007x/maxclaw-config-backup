# Shared Memory Layer

跨 Agent 共享记忆层，用于：
- 项目共同信息
- 用户偏好（跨 Agent）
- 共享资源

## L0 Abstract

所有 Agent 可访问的共享记忆空间。

## L1 Overview

共享记忆层包含：
1. **共享项目信息** - 多 Agent 协作项目的数据
2. **用户全局偏好** - 用户在所有交互中的通用偏好
3. **跨 Agent 知识** - 被多个 Agent 使用的信息

## 生命周期: P0 (持久)
